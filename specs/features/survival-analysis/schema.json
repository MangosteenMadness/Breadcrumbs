{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "breadcrumbs/specs/features/survival-analysis/schema.json",
  "title": "Survival Analysis — composed feature schema",
  "description": "Per-feature schema. It allOf-extends the repo-local feature schema (which references the layer schemas), then pins feature_id. NO ORPHANS: this schema must always extend feature.schema.json.",
  "allOf": [{ "$ref": "../../../.spec/schemas/feature.schema.json" }],
  "required": ["feature_id"],
  "properties": {
    "feature_id": { "const": "survival-analysis" }
  }
}
