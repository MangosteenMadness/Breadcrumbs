{
  "feature_id": "research-memory-tools",
  "scenarios": [
    {
      "id": "SCN-001",
      "title": "Internal-only: duplication checks organizational memory",
      "kind": "happy_path",
      "mode": "public",
      "description": "Breadcrumbs supplies organizational research memory only; general literature research remains with the host agent.",
      "preconditions": ["The graph holds a confirmed finding matching the hypothesis"],
      "steps": [
        "Call check_duplication with a hypothesis that matches an existing internal finding",
        "Inspect the returned fields and named sources"
      ],
      "expected": [
        "A verdict of 'match' is returned, citing the internal finding",
        "Only the internal Breadcrumbs graph is represented in the response"
      ],
      "linked_requirements": ["REQ-003"],
      "evidence_required": ["command-log"]
    },
    {
      "id": "SCN-002",
      "title": "The save: an abandoned attempt is surfaced before a researcher repeats it",
      "kind": "happy_path",
      "mode": "public",
      "description": "This is the demo's punchline and the reason the product exists. A researcher asks a question adjacent to work a teammate already abandoned; the abandoned attempt comes back with its reason, so the researcher does not burn a week rediscovering the same dead end.",
      "preconditions": ["F-093 (abandoned, stage-adjustment collapse) is seeded in the graph"],
      "steps": [
        "From a fresh session with no shared context, ask a question related to the signature F-093 tested",
        "Call recall_findings"
      ],
      "expected": [
        "F-093 is returned and clearly flagged as abandoned",
        "Its reason — the effect collapsed after stage adjustment — is attached",
        "It is not buried beneath confirmed findings merely for being abandoned"
      ],
      "linked_requirements": ["REQ-005", "REQ-008"],
      "evidence_required": ["command-log"]
    },
    {
      "id": "SCN-003",
      "title": "The system never claims novelty",
      "kind": "error",
      "mode": "public",
      "description": "Calibration is the credibility of the whole product. Claiming novelty is a claim the system cannot support, and one overclaim on stage costs more than any missing feature.",
      "preconditions": [],
      "steps": [
        "Call check_duplication with a hypothesis that has no internal or external match",
        "Inspect every user-facing string in the response"
      ],
      "expected": [
        "The response says no prior work was found in the named sources",
        "The sources actually searched are named",
        "The word 'novel' does not appear anywhere in the output"
      ],
      "linked_requirements": ["REQ-006"],
      "evidence_required": ["command-log"]
    },
    {
      "id": "SCN-004",
      "title": "The internal-memory check has no literature-service dependency",
      "kind": "edge_case",
      "mode": "public",
      "description": "Breadcrumbs does not perform general literature research, so its internal-memory path has no literature-service dependency.",
      "preconditions": [],
      "steps": ["Run check_duplication with no network", "Inspect the result and graph schema"],
      "expected": ["The result reports only the internal graph", "No external literature cache table or client is required"],
      "linked_requirements": ["REQ-009"],
      "evidence_required": ["command-log"]
    },
    {
      "id": "SCN-005",
      "title": "A surprising interaction becomes approved organizational knowledge",
      "kind": "happy_path",
      "mode": "public",
      "description": "The host proposes a source-linked constraint, quantifies the before/after belief change from repeated categorical judgments, and presents a Memory Diff. Only the researcher's approved version is persisted.",
      "preconditions": ["The source conversation is already ingested"],
      "steps": [
        "Call score_surprise with the prior and posterior belief samples",
        "Show the proposition, exact quote, action change, and calculated metrics to the researcher",
        "After explicit approval, call write_knowledge",
        "Recall it in a fresh session using its topic and scope"
      ],
      "expected": [
        "The score is reproducible from the stored samples",
        "The stored metrics are server-calculated rather than caller-supplied",
        "Recall returns the approved item with source provenance even when the fresh host adds an unknown inferred scope key",
        "A numeric query value satisfying a stored typed condition is reported as compatible"
      ],
      "linked_requirements": ["REQ-012", "REQ-013", "REQ-014"],
      "evidence_required": ["command-log"]
    },
    {
      "id": "SCN-006",
      "title": "A later correction supersedes but does not erase prior knowledge",
      "kind": "edge_case",
      "mode": "public",
      "description": "Continual organizational memory is an append-only patch sequence: a newly approved belief revision points to the prior item rather than overwriting its evidence.",
      "preconditions": ["An approved knowledge item already exists"],
      "steps": [
        "Approve a belief_revision with supersedes_id pointing at the prior item",
        "Recall active knowledge",
        "Recall again with include_superseded enabled"
      ],
      "expected": [
        "Default recall returns only the new active patch",
        "Audit recall returns both versions and their link"
      ],
      "linked_requirements": ["REQ-013", "REQ-014"],
      "evidence_required": ["command-log"]
    },
    {
      "id": "SCN-007",
      "title": "A fresh agent retrieves a differently worded scoped decision",
      "kind": "edge_case",
      "mode": "public",
      "description": "The host paraphrases an approved decision and invents a plausible method key while asking about pH 6.5. Dense retrieval must bridge the wording change, and unknown inferred scope must not erase the relevant patch.",
      "preconditions": ["An approved synthetic decision is indexed by FTS5 and the pinned local embedding model"],
      "steps": [
        "From a fresh host, call recall_knowledge with the assay, buffer_pH 6.5, and an unstored method key",
        "Inspect the retrieval components and scope compatibility"
      ],
      "expected": [
        "The active patch is returned from local retrieval",
        "buffer_pH is compatible and method is unknown rather than exclusionary",
        "The result exposes its BM25, dense, rank-fusion, token, and applicability components"
      ],
      "linked_requirements": ["REQ-014"],
      "evidence_required": ["command-log"]
    },
    {
      "id": "SCN-008",
      "title": "A fresh agent identifies demonstrated topic expertise with evidence",
      "kind": "happy_path",
      "mode": "public",
      "description": "The host asks who has expertise in or is investigating a topic. Breadcrumbs aggregates authored decisions, findings, and abandoned work across independent sessions while separately surfacing named session owners, without turning reviewer activity, a single question, or message volume into an unsupported expert claim.",
      "preconditions": ["Canonical provisional people have source-linked contributions and named research sessions"],
      "steps": [
        "Call find_experts with a natural-language topic and optional scope",
        "Inspect the ranked people, confidence, score components, and cited evidence"
      ],
      "expected": [
        "The person with stronger independent primary evidence ranks first",
        "Named relevant session owners appear separately as active investigators even without primary evidence",
        "Investigation activity alone never creates a demonstrated expert",
        "Repeated work in one session is capped and review-only or non-person identities are excluded",
        "Explicit disease scope excludes off-disease findings and low-coverage incidental matches",
        "Abandoned work is retained as evidence",
        "The response is concise, limited to stored evidence, and makes no organizational-title or unsupported biological claim"
      ],
      "linked_requirements": ["REQ-015"],
      "evidence_required": ["command-log"]
    }
  ],
  "holdout_suites": []
}
