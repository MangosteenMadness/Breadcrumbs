{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "agenticflow/setupref/layers/_layer.base.schema.json",
  "title": "Layer Base",
  "description": "Fields common to every layer block (frontend/backend/database/contract). Each concrete layer schema extends this via allOf so no layer schema is an orphan.",
  "type": "object",
  "required": ["owner", "status"],
  "properties": {
    "owner": {
      "type": "string",
      "description": "Role or person accountable for this layer of the feature."
    },
    "status": {
      "type": "string",
      "description": "Build status of this layer for the feature.",
      "enum": ["planned", "in_progress", "built-at-parity", "gap", "not-built", "descoped", "unverified"]
    },
    "component_refs": {
      "type": "array",
      "description": "Component IDs (from components.md) this layer realizes.",
      "items": { "type": "string" }
    },
    "notes": { "type": "string" }
  }
}
