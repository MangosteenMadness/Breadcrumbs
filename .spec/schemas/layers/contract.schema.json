{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "agenticflow/setupref/layers/contract.schema.json",
  "title": "Contract Layer",
  "description": "The API boundary / shared-types layer (e.g. shared/src/<ctx>.ts). The contract is the source of truth that database (schema) and backend (endpoints) must stay in parity with.",
  "allOf": [{ "$ref": "./_layer.base.schema.json" }],
  "type": "object",
  "required": ["source", "types"],
  "properties": {
    "source": {
      "type": "string",
      "description": "Path to the contract module, e.g. 'shared/src/sales.ts'."
    },
    "types": {
      "type": "array",
      "description": "Exported type/interface names that form the API boundary.",
      "minItems": 1,
      "items": { "type": "string" }
    },
    "enums": {
      "type": "array",
      "description": "Exported enums / discriminated-union tags.",
      "items": { "type": "string" }
    }
  }
}
