{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "<repo>/specs/features/<feature-id>/schema.json",
  "title": "<Feature Title> — composed feature schema",
  "description": "Per-feature schema. It allOf-extends the setupref feature schema (which references the layer schemas), then pins feature_id and adds any feature-specific constraints. Replace __SCHEMAS__ with the relative path to the repo-local copied schemas, usually '.spec/schemas' from the repo root. NO ORPHANS: this schema must always extend feature.schema.json.",
  "allOf": [{ "$ref": "__SCHEMAS__/feature.schema.json" }],
  "required": ["feature_id"],
  "properties": {
    "feature_id": { "const": "<feature-id>" }
  }
}
