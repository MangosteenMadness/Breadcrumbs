{
  "feature_id": "<feature-id>",
  "scenarios": [
    {
      "id": "SCN-001",
      "title": "<public scenario title>",
      "kind": "happy_path",
      "mode": "public",
      "description": "<visible implementation scenario>",
      "preconditions": [],
      "steps": ["<step 1>", "<step 2>"],
      "expected": ["<observable expected result>"],
      "linked_requirements": ["REQ-001"],
      "evidence_required": ["command-log"]
    }
  ],
  "holdout_suites": [
    {
      "id": "HOLDOUT-001",
      "owner": "qa",
      "path": ".spec/holdouts/<feature-id>/holdout.json",
      "runCommand": "<command that runs hidden/late-loaded checks>",
      "description": "Pointer only. Do not paste hidden scenario contents into implementation prompts."
    }
  ]
}
